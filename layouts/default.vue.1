<template>
  <v-app style="background-color: #fff">
    
      <v-navigation-drawer
        v-model="drawer"
        :mini-variant="miniVariant"
        :clipped="clipped"
        fixed
        app
      >
        <v-list>
          <v-list-item v-for="(item, i) in items" :key="i" :to="item.to" router exact>
            <v-list-item-action>
              <v-icon>{{ item.icon }}</v-icon>
            </v-list-item-action>
            <v-list-item-content>
              <v-list-item-title v-text="item.title" />
            </v-list-item-content>
          </v-list-item>
        </v-list>
      </v-navigation-drawer>
      <v-card class="overflow-hidden" >
      <v-app-bar app style="background: #dfdfdfd" >
      
        <v-app-bar-nav-icon @click.stop="drawer = !drawer"/>
   
        <v-btn icon @click.stop="miniVariant = !miniVariant">
          <v-icon>mdi-{{ `chevron-${miniVariant ? 'right' : 'left'}` }}</v-icon>
        </v-btn>
        <v-btn icon @click.stop="clipped = !clipped">
          <v-icon>mdi-application</v-icon>
        </v-btn>

        <v-toolbar-title v-html="title"/>
        <v-spacer />
        
        <v-btn color="primary" text nuxt to="/" class="mx-1">Dashboard</v-btn>
        <v-btn color="primary" text nuxt to="/acquisitions" class="mx-1">Acquisitions</v-btn>
        <v-btn color="primary" text nuxt to="/terminations" class="mx-1">Terminations</v-btn>
        <v-btn color="primary" text nuxt to="/tasks" class="mx-1">Tasks</v-btn>
        <v-btn color="primary" text nuxt to="/notifications" class="mx-1">Notifications</v-btn>
        <v-btn color="primary" text nuxt to="/reporting" class="mx-1">Reporting</v-btn>
    
        <div v-if="!$auth.id">
        <v-btn color="success" outlined text nuxt to="/login" class="mx-1">Login</v-btn>
        </div>
        <div v-else>
          <div>
            Welcome, {{ $auth.user.attributes.name }}
              <v-btn icon @click="$store.dispatch('auth/logout')">
        <v-icon>mdi-dots-vertical</v-icon>
      </v-btn>
          </div>
        </div>

      </v-app-bar>
      </v-card>
      <v-main>
        <v-container style="background-color: #fff">
         hjkgfjhgfj
        </v-container>
      </v-main>
      
      <v-footer :absolute="fixed" app style="background: linear-gradient(to left, #8e9eab,#eef2f3 ">
        <span>&copy; Vision Agggregation {{ new Date().getFullYear() }}.</span>
      </v-footer>

  </v-app>
</template>

<script></script>

